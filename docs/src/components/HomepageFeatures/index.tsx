import React from "react";

// This is a placeholder to avoid breaking import references
// We're not using this component anymore as it's been replaced with the new components
export default function HomepageFeatures(): React.ReactElement {
  return <></>;
}
